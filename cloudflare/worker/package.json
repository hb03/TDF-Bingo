{
  "name": "bingo-live",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker + Durable Object für den Live-Mehrspielermodus (WebSocket-Räume)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
